Perform credential access against the target environment:
Domain: {{ domain }}
Targets: {{ targets_display }}
DC IP: {{ dc_ip_display }}
Username: {{ user_display }}
Auth: {{ cred_type }} (auto-resolved at dispatch — do NOT pass password/hash/ticket fields)
{% if hash_type -%}
Hash Type: {{ hash_type }}
{% endif -%}
{% if source -%}
Credential Source: {{ source }}
{% endif -%}
{% if reason -%}
Reason: {{ reason }}
{% endif -%}
Techniques: {{ techniques_display }}
Task ID: {{ task_id }}

{% if hash_note -%}
{{ hash_note }}

{% endif -%}
Reference the principal by username + domain only; the worker injects the credential at dispatch. If DC IP is provided, pass -dc-ip to Kerberos/LDAP tools to avoid DNS issues. **PRIORITY ORDER when creds available:**
1. gpp_password_finder + sysvol_script_search (LOW HANGING FRUIT - run first!)
2. Kerberoast for service account hashes
3. secretsdump if admin access exists
4. LSASS dumping if viable
Report any hashes or credentials found.
{% if state_context %}

## Current Operation State

{{ state_context }}
{% endif -%}
