---
# yaml-language-server: $schema=https://raw.githubusercontent.com/cowdogmoo/warpgate/main/schema/warpgate-template.json
metadata:
    name: ares-base
    version: 1.0.0
    description: Base Ares framework image with Python, uv package manager, and core dependencies
    author: Dreadnode <info@dreadnode.io>
    license: MIT
    tags:
        - ares
        - python
        - base
    requires:
        warpgate: '>=1.0.0'
name: ares-base
version: latest
base:
    image: kalilinux/kali-rolling
    pull: true
    privileged: true
    volumes:
        /sys/fs/cgroup: /sys/fs/cgroup:rw
    run_command:
        - -d
        - -i
        - -t
        - --cgroupns=host
        - '{{ .Image }}'
    environment:
        DEBIAN_FRONTEND: noninteractive
        TZ: UTC
    changes:
        - ENTRYPOINT ${ENTRYPOINT}
        - ENV DEBIAN_FRONTEND=noninteractive
        - ENV TZ=UTC
sources:
    - name: ares
      git:
        repository: https://github.com/dreadnode/ares.git
        ref: main
        depth: 1
        auth:
          token: ${GITHUB_TOKEN}
provisioners:
    - type: shell
      only:
        - docker.arm64
        - docker.amd64
      inline:
        - apt-get update
        - apt-get install -y --no-install-recommends ca-certificates git procps sudo python3-apt
        - apt-get install -y --no-install-recommends python3.13 python3.13-venv python3.13-dev python3-pip
        - python3.13 -m pip install --break-system-packages --no-cache-dir uv
        - python3.13 -m pip install --break-system-packages cffi cryptography
        - python3.13 -m pip install --break-system-packages ansible-core
    - type: ansible
      only:
        - docker.arm64
        - docker.amd64
      playbook_path: ${PROVISION_REPO_PATH}/playbooks/ares/base.yml
      galaxy_file: ${PROVISION_REPO_PATH}/requirements.yml
      extra_vars:
        container_build: true
        ansible_shell_executable: /bin/bash
        ansible_python_interpreter: /usr/bin/python3
      ansible_env_vars:
        - ANSIBLE_REMOTE_TMP=/tmp/ansible-tmp-$USER
        - PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
    - type: shell
      only:
        - docker.arm64
        - docker.amd64
      inline:
        - /tmp/post_ansible_cleanup.sh
    - type: file
      source: ${sources.ares}
      destination: /tmp/ares-build
    - type: shell
      only:
        - docker.arm64
        - docker.amd64
      inline:
        - python3.13 -m pip install --break-system-packages --no-cache-dir /tmp/ares-build
        - rm -rf /tmp/ares-build
        - echo "Build completed successfully"
      changes:
        - USER root
        - WORKDIR /root
targets:
    - type: container
      platforms:
        - linux/amd64
        - linux/arm64
